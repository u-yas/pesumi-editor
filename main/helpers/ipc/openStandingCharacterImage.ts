import { rejects } from 'assert'

/**
 * キャラクターの立ち絵をプロジェクトフォルダにインポートする処理
 *  @param Character どのキャラクターの画像なのかを選択する
 */
const openStandingCharacterImage = () => {
  return new Promise((resolve, reject) => {
    // reject({})
    resolve({})
  })
}

export default openStandingCharacterImage
